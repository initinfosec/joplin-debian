const tlsCommon = require("tls");
const origCreateSecureContext = tlsCommon.createSecureContext;
tlsCommon.createSecureContext = function(options, context) {
  console.log(">>> CAUGHT");
  origCreateSecureContext.apply(null, arguments);
};
